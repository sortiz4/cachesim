#pragma once
#include <exception>
#include <stdexcept>

using Exception = std::exception;
using FormatException = std::runtime_error;
using IoException = std::runtime_error;
using RuntimeException = std::runtime_error;
